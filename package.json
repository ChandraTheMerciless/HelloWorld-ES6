{
  "name": "es6-playground",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "buildJsDir": "babel src -d dist",
    "buildJs": "babel src/hello.es6 -o dist/hello.js",
    "build": "npm run buildJsDir && npm run buildJs"
  },
  "author": "Chandra",
  "license": "ISC",
  "dependencies": {
    "babel-cli": "^6.10.1",
    "babel-preset-es2015": "^6.9.0",
    "babel-preset-react": "^6.5.0",
    "browserify": "^13.0.1"
  }
}
